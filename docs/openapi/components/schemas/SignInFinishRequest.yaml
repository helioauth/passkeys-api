type: object
description: >-
  Request to finish the sign-in process. Contains the request ID and public key
  credential with assertion.
properties:
  requestId:
    type: string
    description: Unique identifier for the sign-in request (from `SignInStartResponse`).
  publicKeyCredentialWithAssertion:
    type: string
    description: The public key credential with assertion.
