type: object
description: Request to finish the sign-in process.
properties:
  requestId:
    type: string
    description: Unique identifier for the sign-in request (returned by `/v1/signin/start`).
  publicKeyCredentialWithAssertion:
    type: string
    description: The result of `navigator.credentials.get(options)` serialized as JSON.
