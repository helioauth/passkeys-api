type: object
description: >-
  Request to finish the sign-up process. Contains the request ID and public key
  credential for attestation.
properties:
  requestId:
    type: string
    description: Unique identifier for the sign-up request (from `/v1/signup/start`)
  publicKeyCredential:
    type: string
    description: The result of `navigator.credentials.create(options)` serialized as JSON
