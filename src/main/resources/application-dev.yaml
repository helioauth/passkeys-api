spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/api
    username: api
    password: password

relying-party:
  allow-origin-port: true
  cache:
    expiration: 999m
    max-size: 99999

admin:
  auth:
    api-key: devapikey